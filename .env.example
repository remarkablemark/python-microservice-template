# Optional Database Configuration
# Uncomment and set DATABASE_URL to enable database support

# PostgreSQL (Production)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# SQLite (Development)
# DATABASE_URL=sqlite:///./app.db

# Enable SQL query logging (optional)
# DATABASE_ECHO=true

# Bearer Token Authentication (Optional)
# Set one or more bearer tokens separated by commas to enable authentication
# If not set, protected endpoints will return 500 error
# Example: API_TOKENS=secret-token-123,another-token-456
# API_TOKENS=your-secret-bearer-token-here

# Logging Configuration (Optional)
# Set log level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO
